# תהליך עבודה — סבבי שיפור ושדרוג

המסמך הזה מגדיר איך מתבצע כל סבב שיפור באתר, ומחזיק את ה-backlog המתועדף.
הוא נכתב כדי שכל סוכן או מפתח שממשיך את העבודה יתחיל מאותה נקודה, ולא ינחש.
מקור אמת לכללי העבודה: `CLAUDE.md`. זיכרון מתמשך: טבלת `agent_memory` בפרויקט orzadik.

## המחזור — כל סבב

1. **מדידה.** נתונים חיים מפרויקט Supabase `whtjslgrrfzehivrknuv` בלבד:
   הזמנות ומשפך, עגלות, בריאות הקטלוג (תמונה, מחיר, קטגוריה, תיאור),
   קטגוריות גדולות בלי מדריך, advisors של אבטחה וביצועים, מצב CI והפריסה האחרונה.
2. **בחירה.** פריט אחד או שניים מה-backlog למטה — לפי השפעה מדידה, לא לפי נוחות.
   מה שדורש החלטת בעלים (כסף, משפט, הגדרות בדשבורד) לא מבוצע — נרשם ב"ממתין לבעלים".
3. **בנייה.** שינוי מינימלי וממוקד.
   - לוגיקה: מודול טהור עם בדיקות Vitest.
   - תוכן: מבוסס על נתוני הקטלוג החיים; בלי מחירים בטקסט; מקורות הלכתיים מצוטטים כלשונם;
     היכן שהמנהג משתנה — מתארים ומפנים לרב.
   - מיגרציה: קובץ ב-`supabase/migrations` + החלה על `whtjslgrrfzehivrknuv`, ובדיקה שהשורה
     השמורה זהה לקובץ (md5).
4. **אימות.** `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`,
   ובדיקה חזותית בתצוגה מקומית (mock) בגודל מובייל.
5. **שילוב.** PR → CI ירוק → מיזוג → בדיקה ש-`deploy.yml` הסתיים בהצלחה.
   האתר החי מתעדכן רק מהפריסה ל-Cloudflare; preview של Vercel אינו האתר.
6. **תיעוד.** upsert ל-`agent_memory` (לפי `key`) ועדכון ה-backlog כאן.
7. **דיווח.** מה נעשה, מה נבדק ואיך, ומה ממתין לבעלים.

## כללי ברזל (מתוך CLAUDE.md)

- Supabase: רק `whtjslgrrfzehivrknuv`. לא לגעת ב-SmarPackageAi. לא להריץ `supabase config push`.
- אין `aggregateRating` על ביקורות Google.
- עריכות תוכן בכמות נרשמות בטבלאות ה-ledger.
- טווחי מספרים בעברית — מקף ASCII בלבד.

## Backlog מתועדף

המספרים נמדדו על הנתונים החיים ב-2026-09-26.

| #   | פריט                            | למה                                                                                                  | סטטוס       |
| --- | ------------------------------- | ---------------------------------------------------------------------------------------------------- | ----------- |
| 1   | מדריך נטלה ומים אחרונים         | 267 מוצרים פעילים בלי מדריך; מדריכים הם העמודים הכי חזקים של האתר בגוגל                              | בסבב הנוכחי |
| 2   | מדריך סידורים, ברכונים ותהילים  | כ-210 מוצרים (ברכונים 91, סידורים 79, סידורים ותהילים 43) בלי מדריך; שאלת נוסח היא שאלת קנייה אמיתית | הבא בתור    |
| 3   | הכנה לחנוכה (מתחיל ב-4.12.2026) | עונת המכירות הקרובה; לבדוק את מרכז "מתנות לחנוכה", הבאנר בדף הבית ומדריך החנוכיה עד תחילת נובמבר     | עד 1.11     |
| 4   | מדיניות RLS של `agent_memory`   | אזהרת ביצועים של Supabase (initplan + שתי מדיניות חופפות); תיקון קטן                                 | קטן         |
| 5   | מדריך לחגים (פסח, ראש השנה)     | 525 מוצרים תחת "חגים"; עונתי — לתזמן כחודשיים לפני כל חג                                             | עונתי       |

### הושלם

| מתי        | פריט                                                                      | PR   |
| ---------- | ------------------------------------------------------------------------- | ---- |
| 2026-09-25 | "כבר נמסרה ללקוח" להזמנות שסומנו באיחור; מדריך בוקר ומצב מערכת מפנים אליו | #130 |
| 2026-09-25 | הקרוסלה הוצאה מהחבילה הראשית (269 → 256 KB gzip)                          | #130 |
| 2026-09-26 | מדריך כיפות + סינון כיפות לפי טווח קוטר                                   | #131 |

## ממתין לבעלים

- **הזמנות לסימון:** 260823-3107, 260827-9708, 260907-5614 שולמו ולא סומנו כנשלחו —
  "כבר נמסרה ללקוח" בחלון ההזמנה. הזמנה חדשה 260925-1900 שולמה ב-25.9 וממתינה למשלוח.
- **הסכמה לדיוור לנוטשי קופה:** מי שסימן הסכמה ולא שלח את הטופס — ההסכמה לא נשמרת.
  שינוי כאן הוא החלטה משפטית.
- **תשלומים בקארדקום:** לאשר במסוף תשלומים ללא עמלה ללקוח, ואז להגדיר `VITE_CARDCOM_MAX_PAYMENTS`.
- **Supabase Auth (בדשבורד):** להפעיל הגנה מסיסמאות שדלפו ואימות דו-שלבי לחשבונות מנהל.
